{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.perf-technology/perf-mcp",
  "description": "Fact-check and fix AI outputs. Detects hallucinations via multi-channel verification, validates JSON against schemas with auto-repair, and corrects errors before they reach users.",
  "repository": {
    "url": "https://github.com/Perf-Technology/perf-mcp",
    "source": "github"
  },
  "version": "0.1.0",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "perf-mcp",
      "version": "0.1.0",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "description": "Your Perf API key (get one at dashboard.withperf.pro)",
          "isRequired": true,
          "format": "string",
          "isSecret": true,
          "name": "PERF_API_KEY"
        }
      ]
    }
  ]
}
